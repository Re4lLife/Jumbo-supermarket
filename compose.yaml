services:
 client:
  build: .
  ports:
    - "5173:5173"
  env_file:
      - .env
  develop:
    watch:
      - path: ./package.json
        action: rebuild
      - path: .
        target: /app
        action: sync